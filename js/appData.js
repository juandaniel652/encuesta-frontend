/**
 * appData.js - Archivo de compatibilidad
 * Este archivo mantiene la compatibilidad con el HTML existente
 * y prepara el terreno para la arquitectura modular
 */

// Este archivo puede estar vacío o contener inicializaciones legacy
// que necesites mantener por compatibilidad

console.log('📦 appData.js cargado (modo compatibilidad)');

// Si tienes funcionalidad legacy que necesites mantener,
// puedes agregarla aquí temporalmente